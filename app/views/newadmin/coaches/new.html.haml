#main-container-title
  %span
    创建教练
#main-container-body
  = render "shared/flash"

  = simple_form_for resource || Coach.new, url: newadmin_coaches_path() do |f|
    = render partial: "form", locals: { f: f, ftype: :new  }

