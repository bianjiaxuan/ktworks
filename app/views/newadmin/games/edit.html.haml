#main-container-title
  %span
    编辑赛事
#main-container-body
  = render "shared/flash"

  = simple_form_for resource || Game.new, url: newadmin_game_path(resource) do |f|
    = render partial: "form", locals: { f: f, ftype: :edit  }

