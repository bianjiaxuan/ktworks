#main-container-title
  %span
    创建赛事
#main-container-body
  = render "shared/flash"

  = simple_form_for resource || Game.new, url: newadmin_games_path() do |f|
    = render partial: "form", locals: { f: f, ftype: :new  }

