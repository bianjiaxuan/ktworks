#main-container-title
  %span
    创建相册
#main-container-body
  = render "shared/flash"

  = simple_form_for resource || OnlineCourse.new, url: newadmin_albums_path() do |f|
    = render partial: "form", locals: { f: f, ftype: :new  }
