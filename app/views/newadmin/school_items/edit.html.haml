#main-container-title
  %span
    编辑学校
#main-container-body
  = render "shared/flash"

  = simple_form_for resource, url: newadmin_school_item_path(resource) do |f|
    = render partial: "form", locals: { f: f, ftype: :edit  }

